#include <stddef.h>

#include "parsley_lex.h"


symbol_t p_lex(char* code_input) {

  if(!code_input) return (symbol_t){ '\0', 0, NULL };

  static long unsigned int i = 0;
  while(code_input[i] == ' ') i++;
  return (symbol_t){ code_input[i++], 0, NULL };

}
